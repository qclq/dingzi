import { describe, expect, it } from 'vitest'

describe('frontend foundation', () => {
  it('exposes the expected application shell', () => {
    expect('dingzi-frontend').toBe('dingzi-frontend')
  })
})
